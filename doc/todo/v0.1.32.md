# v0.1.32 — Stabilisation, passe drivers & polish

> **Objectif** : Consolider l'existant (v0.1.31), combler les asymétries entre
> drivers, raffiner l'expérience utilisateur, et intégrer trois features ciblées
> issues de la roadmap v3.

**Scope retenu** : release *équilibrée* — quick wins sûrs + quelques extensions à
valeur élevée, surface de test maîtrisée.

> Il faut refaire des audits plus poussées avant toute implémentation (important)

---

## Note méthodologique (à lire avant d'implémenter)

Deux audits automatiques ont alimenté ce plan. **Plusieurs de leurs alertes se sont
révélées fausses** après vérification dans le code :

- ❌ « ~16 panics dans les mutations » — FAUX. Les `data.columns.get(k).unwrap()`
  itèrent sur des clés issues de la même map (`data.columns.keys()`,
  `primary_key.columns.keys()`) : la clé existe toujours, pas de panic possible.
- ❌ « `peek_foreign_key` manquant sur MySQL/SQL Server/CockroachDB » — FAUX, déjà
  implémenté (`mysql.rs:2197`, `sqlserver.rs:1508`, `cockroachdb.rs:288`).
- ❌ « Cancel impossible sur MySQL (`CancelSupport::None`) » — FAUX,
  `mysql.rs:2293` retourne `CancelSupport::Driver`.

**Règle pour cette release** : chaque item ci-dessous marqué `[à vérifier]` doit être
confirmé dans le code *avant* implémentation. Ne pas faire confiance aux descriptions
sur parole.

Statut des items : ✅ vérifié dans le code · 🔍 à vérifier avant de coder.

---

## 1. Stabilisation

Le socle est sain. Peu de vrais bugs. Items réels :

- [x] 🔍 **ClickHouse — slice string non borné** (`clickhouse/types.rs`,
  `&declared[start..end]`) — *vérifié : faux positif, sécurisé par invariant*. Le
  slice est sûr car `to_ascii_uppercase()` **préserve la longueur en octets** et le
  garde `strip_prefix_paren(&upper, "ARRAY(")` impose un préfixe ASCII + suffixe `)`
  → `start ≤ end` sur frontières de caractères valides. Idem `current[9..]`/`[15..]`
  (gardés par `strip_prefix` ASCII). Comme l'invariant est **non évident** (fragile à
  un futur passage à `to_uppercase`), un test de régression `malformed_types_do_not_panic`
  verrouille « pas de panic sur type tronqué/déséquilibré ». `cargo check --tests` OK.
- [x] ✅ **MongoDB — `supports_transactions()` optimiste** — *vérifié : déjà couvert*.
  Le bouton « begin transaction » est gaté par `!isDocumentBased` (`QueryPanelToolbar.tsx:309`)
  → **déjà masqué pour Mongo** (document-based). Les handlers `begin/commit/rollback`
  (`query.rs:1431/1471/1511`) **vérifient déjà** `supports_transactions_for_session(session)`
  avant d'agir → erreur propre, pas de surprise runtime. Le seul résidu (`capabilities().transactions=true`,
  statique) est cosmétique et **non consommé** pour l'UI Mongo. Rien à coder.
- [x] 🔍 **Durcissement défensif léger** (`proxy.rs`) — *vérifié : faux positif*. `&buf[..n]`
  est toujours valide (`n ≤ buf.len()` = 1024), `from_utf8_lossy` ne panique jamais,
  `lines().next().unwrap_or("")` est sûr. Aucun risque réel → non touché (cf. « ne pas
  sur-investir »).

---

## 2. Passe drivers (asymétries réelles)

> La plupart des « trous » signalés par l'audit étaient déjà comblés. Voici ce qui
> reste réellement à faire après vérification.

- [x] ✅ **Maintenance sur Neon / Supabase / TimescaleDB** — *fait (helper `pg_compat::maintenance_operations` + `pg_compat::run_maintenance`, délégué par les 4 drivers PG ; cargo check OK)*.
  Le trait par défaut renvoie `supports_maintenance()=false` +
  `run_maintenance()=NotSupported` (`traits.rs:584-600`). Seul `postgres.rs:496+`
  l'override. Ces trois wrappers PG héritent donc du défaut → pas de VACUUM /
  ANALYZE / REINDEX alors que la base le permet.
  - **Action** : extraire la logique de `postgres.rs` (run/list maintenance) vers un
    helper `pg_compat::*` réutilisable, puis l'exposer dans `neon.rs`, `supabase.rs`,
    `timescaledb.rs` (override `supports_maintenance` + délégation).
  - *Vérif* : sur une connexion Neon, le menu maintenance liste VACUUM/ANALYZE/REINDEX
    et `run_maintenance` s'exécute sans `NotSupported`.
  - *Effort* : faible (~15-20 lignes/driver après extraction du helper).
  - *Note* : TimescaleDB peut justifier des ops spécifiques (compression de chunks) —
    hors scope ici, basique d'abord.
- [ ] ⏸️ **MongoDB — EXPLAIN** — *différé après audit*. EXPLAIN n'est **pas** une
  mécanique driver : `QueryPanel.tsx:785-799` construit une requête `EXPLAIN`
  spécifique au dialecte SQL et `explainPlanParser.ts` ne parse que les formats JSON
  **PostgreSQL/MySQL**. L'`explain` Mongo renvoie une structure radicalement différente
  (`queryPlanner.winningPlan`, stages `COLLSCAN`/`IXSCAN`) → nécessite construction de
  requête Mongo + **nouvelle branche de parser** + mapping UI. Item moyen-gros, pas un
  quick win → chantier dédié hors de cette release.
- [ ] 🔍 **Redis — streaming** : confirmer que `execute_stream` n'est pas implémenté,
  puis l'ajouter via boucle `SCAN` → `StreamEvent` (cohérence avec les autres drivers
  qui exposent `supports_streaming=true`).
  - *Vérif* : navigation d'un grand keyspace Redis sans charger tout en mémoire.
  - *Effort* : moyen. **Optionnel** — ne garder que si le temps le permet.

---

## 3. UX incrémentale

> Locales fr/en **vérifiées alignées** (1813 clés chacune) — pas de désync. Le travail
> porte sur des chaînes en dur et la cohérence des composants.

- [x] 🔍 **Remplacer les `window.confirm()` natifs** par le design system — *fait*.
  Nouveau primitive promisifié `confirmDialog()` (`src/lib/stores/confirmStore.ts`,
  store externe façon `modalStore`) + hôte unique `ConfirmHost` monté dans `App.tsx`,
  qui rend le `DangerConfirmDialog`. 16 sites `window.confirm`/`confirm()` fire-and-forget
  migrés (logs, history, audit, profiling, contracts, instant API, notebook cell,
  query library, settings backup/transfer, plugins, time travel, useNotebook). Réutilise
  les clés i18n existantes. `tsc` + biome OK.
  - *Exception assumée* : 2 gardes synchrones dans `AppLayout` restent sur `window.confirm`
    (commentées) — `setBeforeCloseTab` (retour booléen synchrone) et la désactivation
    sandbox (flux à 3 issues cancel/discard/keep, pas un simple oui/non).
  - *Note audit* : l'audit se trompait sur `useNotebook.ts:701` et `QueryLibraryModal`
    (en réalité fire-and-forget, donc convertis).
- [x] ✅ **i18n des labels EXPLAIN** (`Results/ExplainPlanView.tsx`) — *fait*. Labels
  (« Filter », « Sort Key », « Total cost », « Estimated rows », « Loops »,
  « Width », « rows », « actual »…) extraits en dur → `t('query.explainPlan.*')`.
  Clés ajoutées aux **9 locales** (en/fr authoritatives + es/de/pt-BR/zh-CN/ja/ko/ru).
  `tsc` + biome OK.
- [x] 🔍 **aria-labels manquants** — *fait (audit sur-compté)*. Vérification : les toggles
  de section `DBTree` portent déjà un texte visible (`<span>{t('dbtree.tables')}</span>` +
  compte) → nom accessible OK ; les boutons « + » de création ont un `title` ; les items
  ont leur nom. `TabButton` (close), `TabGroup` (chevron), `TabListDropdown` ont déjà
  leurs aria-labels. **Seul vrai trou** : le bouton « + » de `TabBar` (icon-only dans un
  `Tooltip`, qui ne fournit pas de nom accessible) → `aria-label` ajouté. Pas d'ajout
  redondant ailleurs.
  - *Vérif* : navigation clavier + lecteur d'écran sur l'arbre et les onglets.
- [ ] 🔍 **États vides avec call-to-action** : Diff « No data », listes vides
  (routines, etc.) → ajouter un bouton/lien d'action quand pertinent.

---

## 4. Extras v3 (retenus)

- [x] **Cross-env diff en un clic** *(Core)* — *fait*. Le menu « Comparer avec… »
  (`TableContextMenu.tsx`) devient un sous-menu listant les autres connexions **même
  driver**, groupées par environnement (production/staging/development), chargées
  paresseusement. La sélection ouvre un diff avec **les deux** sources pré-remplies sur
  la même table ; `useDiffSources` gère l'auto-connexion + auto-exécution. L'item
  « Configurer manuellement… » conserve l'ancien comportement (droite vide). `tsc` +
  biome OK ; 2 clés i18n (`diff.compareManual`, `diff.compareNoConnections`) ajoutées
  aux 9 locales.
  - *Vérif* : depuis une table, un bouton « Comparer avec… » propose les connexions
    d'un autre environnement et ouvre le diff pré-configuré.
  - *Effort* : faible-moyen (réutilisation maximale de l'existant).
- [x] **Suggestions d'index** *(Pro / BUSL-1.1)* — *fait (scope EXPLAIN-driven)*. La
  source « historique » a été **écartée** après audit (historique expurgé + nécessiterait
  un parser SQL front) ; chantier séparé si besoin. Implémenté :
  `src/lib/query/indexSuggestions.ts` (logique pure) + `src/components/Results/IndexSuggestions.tsx`
  (UI), tous deux `BUSL-1.1`. Réutilise `parseExplainPlan` (PG + MySQL/MariaDB) : chaque
  `Seq Scan`/`ALL` coûteux filtrant sur des colonnes simples → suggestion `CREATE INDEX`
  avec SQL prêt à copier (via `quoteIdentifier`). Prédicats fonctionnels/calculés ignorés
  (un index simple ne les couvre pas). Affiché en bannière au-dessus de l'arbre EXPLAIN,
  gaté par `isFeatureEnabled('index_suggestions')` (nouveau flag Pro). 9 maps/locales à
  jour ; extraction de colonnes couverte par 9 cas de test (PG casts/`~~`/ANY, backticks
  MySQL, `IS NULL`, `BETWEEN`, fonction → pas de suggestion). `CLAUDE.md` + `FEATURES.csv`
  mis à jour. `LICENSE-BSL` n'a **pas** besoin d'édition (il identifie les fichiers Premium
  par leur header SPDX, pas par liste). `tsc` + biome OK.
  - *Vérif* : sur une query sans index avec scan séquentiel coûteux, une suggestion
    `CREATE INDEX` pertinente est proposée avec le SQL prêt.
  - *Effort* : moyen-élevé. Commencer par PostgreSQL/MySQL.
- [x] **AI : Summarize results** *(Pro / BUSL-1.1)* — *fait*. Nouveau composant Premium
  `src/components/Notebook/results/CellResultSummary.tsx` (SPDX `BUSL-1.1`), rendu par
  `SqlCell` (Core) sous le résultat. Bouton « Résumer » gaté par
  `isFeatureEnabled('ai') + isReady + sessionId + résultat table`. **Réutilise** la
  commande backend existante `ai_explain_result` (`aiExplainResult`) + `AiResponseDisplay`
  + `useAiPreferences` — **aucun changement backend**. Erreur → bouton réaffiché pour
  retry. Clé i18n `ai.summarizeResults` ajoutée aux 9 locales. `CLAUDE.md` (périmètre
  Premium) mis à jour. `tsc` + biome OK.
  - *Vérif* : bouton « Résumer » sur un résultat de cellule → résumé en langage naturel.
  - *Effort* : moyen.

---

## 5. Checklist de release

- [ ] `pnpm lint:fix` + `pnpm format:write` propres
- [ ] `cargo check` OK (tests lancés par l'utilisateur)
- [ ] Headers SPDX corrects sur tout nouveau fichier (Core `Apache-2.0` / Pro `BUSL-1.1`)
- [ ] `CLAUDE.md` — périmètre Premium mis à jour (Suggestions d'index, AI Summarize)
- [ ] `doc/FEATURES.csv` — nouvelles features ajoutées
- [ ] `README.md` — mention des nouveautés si visibles utilisateur
- [ ] i18n complet sur tout texte ajouté (en.json + fr.json synchronisés)
- [ ] Bump version `package.json` + `src-tauri/Cargo.toml` → `0.1.32`
- [ ] Changelog
