# SPDX-License-Identifier: Apache-2.0
[workspace]
resolver = "2"
members = ["qoredb-plugin-sdk", "query-stats", "slow-query-toast"]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
